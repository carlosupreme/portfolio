export interface ExperiencePreview {
    title: string,
    when: string,
    where: string,
    description: string,
    skills: string[],
    image: string
    podium: "gold" | "silver" | "bronze"
}